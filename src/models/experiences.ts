export type Experiences = {
  title: string,
  subtitle?: string,
  description?: string,
  company?: string,
  year_starting?: number,
  tasks: string[]
  year_ending?: number,
  isPersonalProject?: boolean,
  href?: string,
  project?: string,
}
