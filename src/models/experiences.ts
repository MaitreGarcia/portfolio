export type Experiences = {
  title: string,
  subtitle?: string,
  description?: string,
  company?: string,
  year_started?: number,
  tasks: string[]
  year_ended?: number,
  isPersonalProject?: boolean,
  href?: string,
  project?: string,
}
